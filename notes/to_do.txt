theme picker,
color picker,
extension picker,
app picker.

create a v1 variant.


optional:
backup and restore spotify settings and login



Fix requests error checking, find a way to return the error from the function, and then return to main menu.
Fix deletion of folders permission errors
Move Spicetify Version Selecting to globals.py
for x in section_dict if self.bool == True: